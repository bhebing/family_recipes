services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: family_recipes
      POSTGRES_PASSWORD: family_recipes
      POSTGRES_DB: family_recipes
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
